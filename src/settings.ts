const TIMEOUT = 900;
const RANDOMIZED = true;

enum AppRoute {
  Initial = "initial",
  Game = "game",
  Results = "results",
}

enum AppTheme {
  Cats = "cats",
  Cars = "cars",
  Flowers = "flowers",
}

export { TIMEOUT, RANDOMIZED, AppRoute, AppTheme };
